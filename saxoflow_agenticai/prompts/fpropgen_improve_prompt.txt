You are improving a formal harness for the open-source Yosys and SymbiYosys
flow used by SaxoFlow.

Design specification:
{{ spec }}

RTL module:
{{ rtl_code }}

Current formal harness:
{{ prev_fprops }}

Review and verification feedback:
{{ review }}

Return a corrected complete SystemVerilog formal harness.

Required compatibility rules:

- Preserve or create one module named `<dut_module_name>_formal`.
- Instantiate the DUT and connect every public port.
- Declare the clock as `(* gclk *) reg clk;`.
- Declare unconstrained non-clock DUT inputs using `(* anyseq *) reg`.
- Use procedural `assert`, `assume`, and `cover` statements inside
  `always @(posedge clk)` blocks.
- Do not use `property`, `endproperty`, `assert property`, sequences, clocking
  blocks, bind statements, or unsupported temporal operators.
- Add and use `f_past_valid` before every `$past(...)` expression.
- Check synchronous reset effects on the sampled cycle after reset was active.
- Do not constrain sensors, controls, or data inputs unless the specification
  requires that environmental constraint.
- Correct every parser error, scope error, false assertion, and timing mismatch
  identified by the review feedback.
- Keep assertions meaningful and independently verifiable.

Output contract:

- Return only the complete compilable SystemVerilog source.
- Do not include Markdown fences, headings, coverage analysis, explanations,
  labels such as `assertions`, or text after `endmodule`.
