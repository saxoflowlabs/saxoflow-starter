You are reviewing a SystemVerilog formal harness for the open-source Yosys and
SymbiYosys flow used by SaxoFlow.

Formal harness:
{{ formal_properties }}

Design specification:
{{ spec }}

RTL module:
{{ rtl_code }}

Review requirements:

- Confirm the harness is one complete module that instantiates the DUT.
- Confirm all DUT ports are declared and connected with compatible widths.
- Reject `property`, `endproperty`, `assert property`, sequences, clocking
  blocks, bind statements, and unsupported temporal operators.
- Require procedural `assert`, `assume`, and `cover` statements inside
  clocked `always` blocks.
- Require `(* gclk *)` for the formal clock and `(* anyseq *)` for symbolic
  non-clock inputs.
- Require an `f_past_valid` guard for every use of `$past(...)`.
- Check synchronous reset effects on the sampled cycle after reset was active.
- Check asynchronous reset according to the RTL sensitivity list.
- Flag assertions that can fail only because RTL state is unconstrained before
  reset.
- Flag unjustified assumptions that over-constrain the environment.
- Check that assertions match the specification rather than merely repeating
  the implementation.
- Check legal outputs, mutual exclusion, state transitions, counter boundaries,
  and protocols where applicable.
- Reference public DUT ports where possible. Hierarchical internal references
  must be explicit and justified.

Return exactly these headings:

Missing Properties:
[Issues or "None"]

Trivial Properties Detected:
[Issues or "None"]

Scope & Coverage Issues:
[Issues or "None"]

Cycle-Accuracy Problems:
[Issues or "None"]

Assertion Naming Suggestions:
[Suggestions or "None"]

Overall Property Set Quality:
[State whether it is ready for Yosys/SymbiYosys]

Additional Formal Suggestions:
[Suggestions or "None"]
